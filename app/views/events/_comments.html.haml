%h2 Comments 
- @event.comments.each do |comment|
  
  %p
    %strong Comment:
    = comment.comment
    = current_user.first_name
    = (comment.created_at + 3.hour).strftime("%d.%m.%y %H:%M")

- if user_signed_in?
  = form_for([@event, @event.comments.build]) do |f|
    %p
    = f.label :comment
    %br/
    = f.text_field :comment
    %p
    = f.submit  
- else
  = link_to 'Увійти', new_user_session_path
  или #{link_to 'Зареєструватися', new_user_registration_path}